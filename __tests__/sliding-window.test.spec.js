import { SlidingWindow } from '../src/sliding-window';

describe('SlidingWindow', () => {
  /** This is a useless test. It's here as a skeleton for the SlidingWindow unit tests. */
  it('the slidingWindow instance is not null', () => {
    let slidingWindow = new SlidingWindow();
    expect(slidingWindow).not.toEqual(undefined);
  });
});
